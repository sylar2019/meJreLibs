apply from: gradlesPath + "springcloud.gradle"

dependencies {
    compile project(":utils:utils-base")
    compile "com.ecwid.consul:consul-api:${consulVersion}"
    compile "org.apache.thrift:libthrift:${thriftVersion}"
}
